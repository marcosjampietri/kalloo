import styled from "styled-components";

const Items = () => {
    return (
        <>
            <Section>OUTRA SEÇÃO</Section>
        </>
    );
};

export default Items;

const Section = styled.section`
    width: 100%;
    height: 700px;

    background: hsla(0, 0%, 75%, 1);
`;
